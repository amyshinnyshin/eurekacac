from django.contrib import admin
from .models import ConsultationContact, ConsultationDateRange

# Register your models here.
admin.site.register(ConsultationContact)
admin.site.register(ConsultationDateRange)
